from __future__ import annotations

import typing

import clientele
{% if client_project_directory_path %}from {{client_project_directory_path}} import config, schemas
{% else %}from . import config, schemas
{% endif %}
client = clientele.Client(config=config.Config())
